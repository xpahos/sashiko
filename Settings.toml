log_level = "info"

[database]
url = "sashiko.db"
token = ""

[mailing_lists]
track = ["bpf", "linux-mm:org.kernel.linux-mm"]

[nntp]
server = "nntp.lore.kernel.org"
port = 119

[ai]
provider = "gemini"
model = "gemini-3-flash-preview"
max_input_tokens = 200000
max_interactions = 100
temperature = 1.0
explicit_prompts_caching = true

[server]
host = "127.0.0.1"
port = 8081

[git]
repository_path = "third_party/linux"

[review]
concurrency = 1
worktree_dir = "review_trees"
timeout_seconds = 3600
max_retries = 3